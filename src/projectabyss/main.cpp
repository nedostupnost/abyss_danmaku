#include "../libprojectabyss/core/Game.h"

// Точка входа в приложение
// Последовательность действий:
// 1. Создание экземпляра игры
// 2. Запуск основного игрового цикла
// 3. Завершение программы с кодом возврата
int main() {
    // Создаем основной игровой объект, который будет управлять всем игровым процессом
    Game game;

    // Запускаем основной игровой цикл
    // Внутри этого метода происходит:
    // - Обработка событий
    // - Обновление игровых объектов
    // - Отрисовка кадров
    game.run();

    // Возвращаем 0 как индикатор успешного завершения программы
    return 0;
}